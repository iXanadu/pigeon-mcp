# Non-sensitive configuration — safe to reference in docs, NOT committed when populated.
# Copy to .env and fill in values.

ENVIRONMENT=local
PROJECTNAME_LOG_LEVEL=info

# Database (only if this project needs one — see /init step "Database — MANUAL")
PROJECTNAME_DB_HOST=localhost
PROJECTNAME_DB_PORT=5432
PROJECTNAME_DB_NAME=projectname_dev
PROJECTNAME_DB_USER=projectname
